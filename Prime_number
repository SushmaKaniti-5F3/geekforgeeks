class Solution{
public:
    int isPrime(int N){
        if(N==1)return 0;
        int count=0;
        for(int i=1;i*i<=N;i++)
        {
            if(N%i==0)
            count++;
        }
        if(count==1)
        return 1;
        else
        return 0;
        
    }
};
